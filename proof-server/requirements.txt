fastapi==0.115.0
uvicorn[standard]==0.30.6
httpx==0.27.2
python-dotenv==1.0.1
pydantic==2.9.2

# Optional — only needed for real on-chain signal fetching
# web3==7.2.0

# Optional — only needed for real EZKL ZK proofs (installed separately via venv)
# ezkl     →  pip install ezkl
# torch    →  pip install torch --index-url https://download.pytorch.org/whl/cpu
# eth_abi  →  pip install eth_abi   (decodes EZKL's verifier calldata into proof+instances)
