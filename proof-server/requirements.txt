fastapi==0.115.0
uvicorn[standard]==0.30.6
httpx==0.27.2
python-dotenv==1.0.1
pydantic==2.9.2

# Needed for real on-chain signal fetching (routes/score.py) and to decode
# EZKL's verifier calldata into proof+instances (services/ezkl_service.py).
# Both are lightweight — unlike torch/ezkl below they don't need a separate
# install step, so they're real requirements, not optional.
web3==7.2.0
eth_abi

# Optional — only needed for real EZKL ZK proofs (installed separately via venv)
# ezkl     →  pip install ezkl
# torch    →  pip install torch --index-url https://download.pytorch.org/whl/cpu
